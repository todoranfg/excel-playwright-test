{
  "name": "excel-today-e2e",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "typescript": "^5.5.0",
    "ts-node": "^10.9.2"
  },
  "scripts": {
    "pw:install": "playwright install chrome",
    "login:save": "ts-node scripts/save-login.ts",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  }
}